name: CI
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env: {POSTGRES_DB: datapilot_test, POSTGRES_USER: datapilot, POSTGRES_PASSWORD: datapilot_test}
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U datapilot -d datapilot_test" --health-interval 5s --health-timeout 5s --health-retries 10
    defaults: {run: {working-directory: backend}}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12", cache: pip, cache-dependency-path: backend/requirements.txt}
      - run: pip install -r requirements.txt pip-audit
      - run: python -m pytest
      - run: alembic upgrade head
        env: {DATABASE_URL: "postgresql+psycopg://datapilot:datapilot_test@localhost:5432/datapilot_test", APP_ENV: test}
      - run: alembic upgrade 0005 && alembic upgrade head
        env: {DATABASE_URL: "sqlite:///./migration-upgrade-test.db", APP_ENV: test}
      - run: pip-audit -r requirements.txt
  frontend:
    runs-on: ubuntu-latest
    defaults: {run: {working-directory: frontend}}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22, cache: npm, cache-dependency-path: frontend/package-lock.json}
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run build
      - run: npm audit --omit=dev
  repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git diff --check
