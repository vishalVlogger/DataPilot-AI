"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/auth/AuthContext";
import { getProviderStatus, getUsage, inviteMember, Invitation, listInvitations, listMembers, Member, ProviderStatus, removeMember, resendVerification, revokeInvitation, updateMemberRole, updateUser, updateWorkspace, type Usage } from "@/services/api";

export default function SettingsPage(){const auth=useAuth(),router=useRouter();const[usage,setUsage]=useState<Usage|null>(null),[provider,setProvider]=useState<ProviderStatus|null>(null),[members,setMembers]=useState<Member[]>([]),[invitations,setInvitations]=useState<Invitation[]>([]),[name,setName]=useState(""),[workspaceName,setWorkspaceName]=useState(""),[inviteEmail,setInviteEmail]=useState(""),[inviteRole,setInviteRole]=useState<"admin"|"member">("member"),[externalAi,setExternalAi]=useState(true),[message,setMessage]=useState(""),[error,setError]=useState("");const manager=auth.workspace?.role==="owner"||auth.workspace?.role==="admin";async function load(){if(!auth.workspace)return;const[foundUsage,foundProvider,foundMembers]=await Promise.all([getUsage(),getProviderStatus(),listMembers(auth.workspace.id)]);setUsage(foundUsage);setProvider(foundProvider);setMembers(foundMembers);setExternalAi(foundProvider.external_ai_enabled);if(manager)setInvitations(await listInvitations(auth.workspace.id));}useEffect(()=>{if(!auth.loading&&!auth.user)router.replace("/login");if(auth.user&&auth.workspace){setName(auth.user.display_name);setWorkspaceName(auth.workspace.name);load().catch(reason=>setError(reason instanceof Error?reason.message:"Unable to load settings"));}},[auth.loading,auth.user,auth.workspace?.id,router]);async function save(event:FormEvent){event.preventDefault();if(!auth.workspace)return;setError("");try{await Promise.all([updateUser(name),updateWorkspace(auth.workspace.id,{name:workspaceName,...(manager?{external_ai_enabled:externalAi}:{})})]);await auth.refresh();setMessage("Settings saved.");}catch(reason){setError(reason instanceof Error?reason.message:"Unable to save settings");}}async function invite(event:FormEvent){event.preventDefault();if(!auth.workspace)return;try{await inviteMember(auth.workspace.id,inviteEmail,inviteRole);setInviteEmail("");setInvitations(await listInvitations(auth.workspace.id));setMessage("Invitation sent.");}catch(reason){setError(reason instanceof Error?reason.message:"Unable to invite member");}}if(auth.loading||!auth.user)return <div className="page-loader">Loading settings…</div>;return <div className="simple-page"><header><div><p className="eyebrow">ACCOUNT & WORKSPACE</p><h1>Settings</h1><p>Manage your beta account, collaborators, privacy, and usage.</p></div><div className="account-tools">{auth.user.is_system_admin&&<Link className="button-link" href="/admin">Admin support</Link>}<Link className="button-link" href="/">Back to dashboard</Link></div></header>{message&&<div className="success-banner">{message}</div>}{error&&<div className="error">{error}</div>}{!auth.user.email_verified_at&&<div className="panel"><h2>Verify your email</h2><p>Verification is required for workspace invitations and external AI access.</p><button onClick={async()=>setMessage((await resendVerification()).message)}>Resend verification</button></div>}<div className="settings-grid"><form className="panel settings-form" onSubmit={save}><h2>Profile and workspace</h2><label>Display name<input value={name} onChange={event=>setName(event.target.value)}/></label><label>Workspace name<input value={workspaceName} disabled={!manager} onChange={event=>setWorkspaceName(event.target.value)}/></label><label>Workspace slug<input disabled value={auth.workspace?.slug??""}/></label>{manager&&<label className="check"><input type="checkbox" checked={externalAi} onChange={event=>setExternalAi(event.target.checked)}/> Allow configured external AI provider</label>}<button>Save changes</button></form>{usage&&<article className="panel"><p className="eyebrow">{usage.plan_code.toUpperCase()} PLAN</p><h2>Monthly usage</h2>{[["Datasets",usage.datasets,usage.limits.datasets],["Analyses",usage.analyses_this_month,usage.limits.analyses_per_month],["AI requests",usage.ai_requests_this_month,usage.limits.ai_requests_per_month],["Reports",usage.reports_this_month,usage.limits.reports_per_month]].map(([label,value,limit])=><div className="usage-row" key={String(label)}><div><b>{label}</b><span>{value} / {limit}</span></div><progress value={Number(value)} max={Number(limit)}/></div>)}</article>}</div>{provider&&<article className="panel"><p className="eyebrow">AI PRIVACY · {provider.app_version}</p><h2>Provider status: {provider.effective_provider}</h2><p>{provider.privacy_notice}</p>{provider.configured_provider!==provider.effective_provider&&<p className="technical-meta">Configured {provider.configured_provider}; using deterministic Mock because workspace policy or verification restricts external access.</p>}</article>}<section className="panel"><h2>Members</h2><div className="version-list">{members.map(member=><div key={member.user_id}><span>{member.role}</span><div><h3>{member.display_name}</h3><p>{member.email} · Joined {new Date(member.joined_at).toLocaleDateString()}</p></div>{manager&&member.role!=="owner"&&<><select value={member.role} onChange={async event=>{await updateMemberRole(auth.workspace!.id,member.user_id,event.target.value as "admin"|"member");await load();}}><option value="member">member</option><option value="admin">admin</option></select><button className="danger" onClick={async()=>{if(confirm("Remove this workspace member?")){await removeMember(auth.workspace!.id,member.user_id);await load();}}}>Remove</button></>}</div>)}</div></section>{manager&&<section className="panel"><h2>Pending invitations</h2><form className="report-options" onSubmit={invite}><label>Email<input type="email" required value={inviteEmail} onChange={event=>setInviteEmail(event.target.value)}/></label><label>Role<select value={inviteRole} onChange={event=>setInviteRole(event.target.value as "admin"|"member")}><option value="member">member</option><option value="admin">admin</option></select></label><button>Send invitation</button></form><div className="version-list">{invitations.filter(item=>!item.accepted_at&&!item.revoked_at).map(item=><div key={item.id}><span>{item.role}</span><div><h3>{item.email}</h3><p>Expires {new Date(item.expires_at).toLocaleString()}</p></div><button className="danger" onClick={async()=>{await revokeInvitation(auth.workspace!.id,item.id);await load();}}>Revoke</button></div>)}</div></section>}<article className="panel"><h2>Beta data deletion</h2><p>Dataset deletion is available from the dashboard with explicit confirmation. During beta, account or entire-workspace deletion is handled by support so identity, tenancy, and audit metadata are not orphaned.</p></article></div>}
