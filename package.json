{
  "name": "datapilot-ai",
  "version": "0.7.0-beta",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
